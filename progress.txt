# Ralph Progress Log
Started: Sat, 04 Apr 2026 14:29:02 BST

## Codebase Patterns
- Flutter binary is at `/home/henry/flutter/bin/flutter` (not on PATH)
- Widgets go in `lib/widgets/`, tests in `test/unit/`
- Extract pure logic into static helper classes for testability (e.g., `ExercisePickerHelper`)
- Exercise model uses `WorkoutType` enum for categories, string lists for muscles
- Pre-existing lint warnings exist in codebase — only fix issues in new/changed code
- Use `fake_cloud_firestore` package for Firestore service unit tests — inject via constructor parameter
- CustomExercise extends Exercise — use polymorphically wherever Exercise is accepted
- Providers go in `lib/providers/`, use StateNotifier<T> pattern with injectable Firestore
- WorkoutLog.edits is optional — existing code/services ignore it; only TodayWorkoutNotifier writes it
- PlanEdits tracks added/removed/swapped separately; removing a user-added exercise cleans up added list (not removed)
---

## 2026-04-04 - US-021
- Implemented ExercisePicker bottom sheet widget with search, category grouping, and exclusion
- Created ExercisePickerHelper with static pure functions: filterExercises, groupByCategory, getCategoryDisplayName
- Files changed: `lib/widgets/exercise_picker.dart` (new), `test/unit/exercise_picker_test.dart` (new)
- **Patterns added to Codebase Patterns:** Flutter binary path, widget/test locations, helper class pattern, Exercise model details, lint policy
- **Learnings for future iterations:**
  - ExerciseService loads from assets/exercises.json and caches — tests that need exercises must call loadExercises() first or use test data directly
  - WorkoutType enum maps to category groupings: push/pull/legs/core/cardio/rest
  - Today screen uses static mock data (not yet connected to services) — future stories will need to integrate
---

## 2026-04-04 - US-028
- Implemented CustomExercise model extending Exercise with isCustom, notes, createdAt fields
- Implemented CustomExerciseService with CRUD operations, Firestore subcollection storage, and case-insensitive duplicate name validation
- Added fake_cloud_firestore dev dependency for Firestore unit testing
- Files changed: `lib/models/custom_exercise.dart` (new), `lib/services/custom_exercise_service.dart` (new), `test/unit/custom_exercise_service_test.dart` (new), `test/unit/custom_exercise_e2e_test.dart` (new), `pubspec.yaml` (updated)
- **Patterns added to Codebase Patterns:** fake_cloud_firestore for Firestore testing, injectable Firestore pattern
- **Learnings for future iterations:**
  - Use `fake_cloud_firestore` package with constructor injection (`CustomExerciseService(firestore: fakeFirestore)`) for testing Firestore services
  - CustomExercise extends Exercise — can be used anywhere Exercise is accepted (polymorphism)
  - Firestore subcollection path: `users/{userId}/custom_exercises/{exerciseId}`
  - pubspec.lock is in .gitignore — don't try to stage it
---

## 2026-04-04 - US-029
- Implemented PlanEdits model (SwapRecord, added/removed/swapped tracking) with JSON serialization
- Implemented TodayWorkoutState and TodayWorkoutNotifier (Riverpod StateNotifier) with add, remove, swap, undoRemove, persistEdits methods
- Updated WorkoutLog model with optional `edits` field — existing services remain unaffected
- Static getLoggedExerciseIds method ensures fatigue is calculated from actually logged exercises only
- Files changed: `lib/models/plan_edits.dart` (new), `lib/providers/today_workout_notifier.dart` (new), `lib/models/workout_log.dart` (updated), `test/unit/today_workout_notifier_test.dart` (new)
- **Patterns added to Codebase Patterns:** Provider directory, StateNotifier pattern, WorkoutLog.edits optionality, PlanEdits removal cleanup logic
- **Learnings for future iterations:**
  - Providers go in `lib/providers/` — first Riverpod state in the project
  - TodayWorkoutNotifier uses constructor injection for Firestore (same pattern as services)
  - When removing a user-added exercise, clean it from edits.added (not edits.removed) to avoid double-tracking
  - WorkoutLog.edits is null-safe — old documents without edits field parse fine
  - RecommendationService and FatigueService read from fatigue collection and workout exercises array — edits field is invisible to them by design
---
